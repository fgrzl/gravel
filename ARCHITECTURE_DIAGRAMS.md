# Architecture Visualization

## Local-Only Mode

```
???????????????????????????????????????????
?          Gravel Database                ?
?        (LocalOnly Storage)              ?
???????????????????????????????????????????
         ?                    ?
    ??????????????    ????????????????
    ? LocalWAL   ?    ? LocalSST     ?
    ? (/disk)    ?    ? Manager      ?
    ?            ?    ? (/disk)      ?
    ? Segments   ?    ?              ?
    ? on disk    ?    ? Level 0..N   ?
    ??????????????    ????????????????
         ?                    ?
    ??????????????????????????????????
    ?    Local Filesystem            ?
    ?  (Durability via fsync)        ?
    ??????????????????????????????????
```

## Hybrid Cloud Mode

```
????????????????????????????????????????????????????????
?               Gravel Database                        ?
?            (HybridCloud Storage)                     ?
????????????????????????????????????????????????????????
    ?                                       ?
???????????????????????          ????????????????????????
?  HybridCloudWAL     ?          ? HybridCloudSST       ?
?                     ?          ? Manager (LRU Cache)  ?
? [Mem Buffer] ? Roll ?          ?                      ?
? ?                   ?          ? [Cloud]??[NVMe]      ?
? Queue Upload Msg    ?          ?                      ?
? (to Actor)          ?          ? Pin/Unpin support    ?
???????????????????????          ????????????????????????
    ?                                      ?
    ?         Posts to Actor               ?
    ?         Runtime                      ?
    ????????????????????????????????????????
                     ?
        ??????????????????????????
        ? ActorRuntime           ?
        ?                        ?
        ? • Retry logic          ?
        ? • Rate limiting        ?
        ? • Bandwidth mgmt       ?
        ? • Ordering guarantee   ?
        ??????????????????????????
                     ?
        ??????????????????????????
        ?   Cloud Storage        ?
        ? (S3/Azure/GCS)         ?
        ?                        ?
        ? • WAL segments         ?
        ? • SST files            ?
        ? • Manifest             ?
        ? • Source of Truth      ?
        ??????????????????????????
```

## Data Flow: Write in Hybrid Cloud

```
                     Write Request
                           ?
                    ????????????????
                    ? WAL Buffer   ?  (Memory, ~10MB)
                    ? (In-Memory)  ?
                    ????????????????
                      ?
                 [Buffer full?]
                   ?        ?
                  NO       YES
                   ?        ??? Roll Segment
                   ?            ?
                   ?      Save to /nvme/cache
                   ?            ?
                   ?      Queue Upload Message
                   ?            ?
           [Return to Client]   ?
           (Latency: ~500µs)    ?
                                ?
                        ?????????????????????
                        ? Actor Runtime     ?
                        ? (Background)      ?
                        ?                   ?
                        ? Upload /nvme/seg  ?
                        ? to Cloud with     ?
                        ? Retries           ?
                        ?????????????????????
```

## Data Flow: Recovery in Hybrid Cloud

```
    Start Recovery
           ?
    ???????????????????
    ?  Cloud Storage  ?
    ?  (Source Truth) ?
    ???????????????????
      ?        ?         ?
   [Manifest] [SST]   [WAL Segs]
      ?        ?         ?
      ????????????????????
             ?
    ??????????????????????
    ? Load Manifest      ?  (Which SSTs exist)
    ? Restore SSTs       ?  (To cache if space)
    ? Load SST Readers   ?
    ??????????????????????
             ?
    ??????????????????????
    ? Replay WAL Segments?  (In sequence)
    ? in Order           ?
    ?                    ?
    ? Entry1 ? Apply     ?
    ? Entry2 ? Apply     ?
    ? ...                ?
    ??????????????????????
             ?
    ??????????????????????
    ? State Recovered    ?
    ? (Identical to      ?
    ?  before crash)     ?
    ??????????????????????
```

## TLV Format

```
One Entry:
????????????????????????????????????????????
?Ty?KeyLen?ValLen?    Key    ?    Value    ?
????????????????????????????????????????????
?1B? 4B   ? 4B   ?  variable ?  variable   ?
????????????????????????????????????????????
 ?
Type: 0x01=Put, 0x02=Delete, 0x03=DeleteRange, 0xFF=End

Total Overhead: 9 bytes per entry
Zero-Copy: Reader returns slices, no allocations
```

## Storage Mode Comparison

```
???????????????????????????????????????????????????
? Aspect       ? Local-Only    ? Hybrid Cloud     ?
???????????????????????????????????????????????????
? Source Truth ? Local Disk    ? Cloud            ?
? Write Speed  ? Fast          ? Very Fast        ?
? Durability   ? Local fsync   ? Cloud + async    ?
? Replay Path  ? Local WAL     ? Cloud WAL        ?
? Cache        ? N/A           ? Ephemeral NVMe   ?
? Failover     ? Manual restore? Automatic (cloud)?
? Scale        ? Single node   ? Multi-node       ?
? Cost         ? Local storage ? Cloud + NVMe     ?
???????????????????????????????????????????????????
```

## Actor Integration Points

```
                    ????????????????????
                    ?  Gravel Engine   ?
                    ????????????????????
                              ?
                    ????????????????????
                    ? Storage Layer    ?
                    ? (TLV Format)     ?
                    ????????????????????
                              ?
                    ????????????????????
                    ? HybridCloudWAL   ?
                    ?                  ?
                    ? WAL Full?        ?
                    ? ? Post Message   ?
                    ????????????????????
                              ?
                    ????????????????????
                    ? ActorRuntime     ?
                    ?                  ?
                    ? Receives:        ?
                    ? Upload Msg       ?
                    ? ? Retries        ?
                    ? ? Rate limits    ?
                    ? ? Bandwidth mgmt ?
                    ????????????????????
                              ?
                    ????????????????????
                    ? Cloud Storage    ?
                    ? (Durable copy)   ?
                    ????????????????????

Benefits:
• Write path stays <1ms (no cloud wait)
• Durability guaranteed by actor retry logic
• Ordering preserved (actor serializes uploads)
• No data loss (crash recovers from cloud)
```

## Cache Lifecycle: Hybrid Cloud SST

```
Scenario: Load SST, Cache Full, Crash

1. Request SST
   ?
2. Cache Miss ? Download from Cloud
   ?
3. Try to Cache
   ?? Space available? ? Add to cache
   ?? No space? ? Evict LRU (if not pinned)
   ?
4. Using SST
   ?? Access ? Update LRU timestamp
   ?
5. Crash! (Cache gone)
   ?
6. Recovery
   ?? Manifest says "need SST-123"
   ?? Cloud has SST-123
   ?? Re-download ? State identical

Result: Cache is optimization, not requirement
```
